# 📄 Pages

Páginas completas de la aplicación. Cada archivo representa una ruta diferente.

## Estructura

- `HomePage.tsx` - Página principal (ruta: `/`)
- `ConnectPage.tsx` - Página QR para eventos (rutas: `/connect`, `/qr`)

## Convención

- Cada página debe ser exportada desde `index.ts`
- Las páginas componen secciones y componentes
- No incluyen lógica de negocio compleja (usar hooks/lib)
